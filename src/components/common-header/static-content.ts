import { HeaderData } from "@/lib/header";

const staticContent: HeaderData = {
  navigationItems: [
    {
      key: 'about',
      label: 'Sobre',
      href: '/about',
      items: [],
    },
  ]
};

export default staticContent;
