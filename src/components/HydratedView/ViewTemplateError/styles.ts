import { Box, Container, styled, Typography } from "@mui/material";

const Wrapper = styled(Container)``;
const TopRow = styled(Box)``;
const BottomRow = styled(Box)``;
const DefaultMessage = styled(Typography)``;
const ErrorLabelMessage = styled(Typography)``;
const DisclaimerMessage = styled(Typography)``;
const UserActionPanel = styled(Box)``;

export default {
	Wrapper,
	TopRow,
	BottomRow,
	DefaultMessage,
	ErrorLabelMessage,
	DisclaimerMessage,
	UserActionPanel,
};
