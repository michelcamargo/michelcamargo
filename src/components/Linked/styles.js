import styled from 'styled-components';

const Embed = styled.a`
  font-size: 1.2em;
  color: var(--black);
  opacity: .8;

  transition: 300ms;

  &:hover{
    opacity: 1;
  }
`;

export default Embed;
