import styled from 'styled-components';

const Embed = styled.a`
  font-size: 1.2em;
  color: blue;
  background: yellow;
  opacity: .7;

  transition: 300ms;

  &:hover{
    opacity: 1;
  }
`;

export default Embed;
