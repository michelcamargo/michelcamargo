import React from 'react';
import { BaseFooter, LabelFooter } from './styles';

export default function Footer() {
  return (
    <BaseFooter>
      <LabelFooter>
        Rodapé
      </LabelFooter>
    </BaseFooter>
  );
}
