import { ServerViewProps } from "@/lib/datahooks";

export default {
  metadata: {
    path: '/not-found',
    title: 'Página não encontrada',
  },
  content: {
    sessions: []
  },
} as ServerViewProps;
